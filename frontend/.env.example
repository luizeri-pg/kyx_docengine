# URL do backend (opcional). Em dev, `http://localhost:3000` é ignorado — usa-se o proxy /api (evita CORS).
# Para outra API em dev (ex.: staging), defina a URL completa.
# VITE_API_URL=https://sua-api.exemplo.com
